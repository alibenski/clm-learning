/**
 * @file
 * clm_theme behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.clmTheme = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
